{
  "name": "adam",
  "version": "1.0.0",
  "description": "A startup template for ADAM projects",
  "main": "index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "serve": "./node_modules/.bin/http-server -a localhost -p 8080 -c-1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "http-server": "^14.1.1"
  }
}
